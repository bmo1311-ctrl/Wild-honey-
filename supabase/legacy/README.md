# Historical, not current

These eleven files were the schema up to roughly 8 August 2026. They are about
a quarter of the real migration history and they do not describe the database
as it is today — see `../README.md`.

Do not run them against a live project and do not read them to find out what a
table looks like. `../schema-snapshot.txt` is the accurate record of the
current shape.
