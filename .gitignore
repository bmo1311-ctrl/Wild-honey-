# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
tsconfig.tsbuildinfo

# local scratch (undeletable in this sandbox)
simhair.ts
sim.tsconfig.json
shapecheck.ts
shape.tsconfig.json
catcheck.ts
cat.tsconfig.json
studiocheck.ts
studio.tsconfig.json

# Sandbox artifacts from editing a mounted folder.
.fuse_hidden*
**/.fuse_hidden*

# Supabase CLI working files. .temp holds the linked project's local state and
# can contain connection details — it must never be committed.
supabase/.temp/
supabase/.branches/

# Next 16 uses proxy.ts, not middleware.ts — having both is a build error.
/middleware.ts
.sim

# Removed — see the note inside. Sandbox cannot delete; safe to remove by hand.
lib/nudges.ts
components/pillar-shelf.tsx
